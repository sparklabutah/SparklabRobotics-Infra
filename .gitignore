# Checkpoints & datasets — push weights to the HF Hub, not here.
MolmoAct2/
checkpoints/
outputs/
datasets/
*.safetensors
*.ckpt
*.pt
*.pth
*.bin
*.parquet

# Secrets & key material. The .bak-<ts> pattern is what make_certs.sh writes.
.env
.env.*
!.env.example
certs/
*.pem
*.key
*.crt
*.p12
*.bak-*

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Scratch
.ipynb_checkpoints/
*.bak
*.orig
*.rej

# Logs & run artefacts
logs/
*.log
MUJOCO_LOG.TXT
wandb/

# Claude Code: share project settings, keep personal overrides local.
.claude/*
!.claude/settings.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Meshes ARE tracked (robots/*/model/assets/*.stl): the MJCF is useless
# without them. Move to git-lfs if they start churning.

# Build artifact from sparklab_sim.convert, derived from the vendored URDF.
src/lerobot_robot_sparklab/robots/yam_ultra/model/usd/
